import express from "express";
import {  } from "../controllers/tipos_producto.js";

const router = express.Router();

router.get("/", );
router.get("/:id", );
router.post("/", );
router.put("/:id", );
router.delete("/:id", );